server:
  port: 8080




spring:
  application:
    name: api-server

  threads:
    virtual:
      enabled: true
#      enabled: false

servers: # 타 서버들의 정보 기록
  consumer: qqueueing-consumer:8082
  target: # 테스트용 사용자 서버(tes 24)
    url: 70.12.247.125:8080/tes24
    traffic-capacity: 200

kafka:
  topic-names:
    enter: test
