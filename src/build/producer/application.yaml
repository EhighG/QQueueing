server:
  port: 8081

  tomcat:
    accept-count: 30000
    max-keep-alive-requests: -1
    max-connections: 30000 # default 8192
    #    connection-timeout: # default 60000 ms
    threads:
      max: 400 # default 200 / thread 경합..? 주의
      min-spare: 200

spring:
  application:
    name: producer-server

kafka:
  topic-names:
    enter: test
